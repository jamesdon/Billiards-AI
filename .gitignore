# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
.env
.env.*
!.env.example

# Tools
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store

# Phase scripts / local calibration outputs
.phase2_*.log
calibration.json
calibration_invalid.json
scripts/*.bak.*

# Ultralytics training runs (local)
runs/

# Local YOLO datasets under data/datasets/ (see also specific lines below)
data/datasets/

# Web setup wizard progress (backend /setup)
data/setup_wizard_progress.json

# Local runtime / large artifacts (adjust if you need to version samples)
*.onnx
*.engine
*.trt
models/checkpoints/

# (data/datasets/ ignored above — billiards-data.yaml, _imports/, images/, labels/, etc.)

# Roboflow Universe: local manifest copies (example *.yaml stays tracked)
scripts/roboflow_universe_manifest.yaml
scripts/roboflow_merge_batch.yaml
