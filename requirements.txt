# Edge / Jetson Orin Nano (aarch64): use NVIDIA's onnxruntime-gpu wheel for your JetPack
# (CUDA + TensorRT execution providers). The PyPI `onnxruntime` package is CPU-only on device.
# See docs/MODEL_OPTIMIZATION.md and docs/DEPLOYMENT_JETSON.md.
#
# aarch64/arm64: pip opencv-python is omitted on purpose — use apt `python3-opencv` (GStreamer for CSI)
# with `python3 -m venv --system-site-packages`. README "Jetson / aarch64" and docs/DEPLOYMENT_JETSON.md.
numpy<2; platform_machine == "aarch64" or platform_machine == "arm64"
numpy; platform_machine != "aarch64" and platform_machine != "arm64"
opencv-python; platform_machine != "aarch64" and platform_machine != "arm64"
onnxruntime
fastapi
uvicorn
pydantic
orjson
pytest
httpx
markdown
pytest-timeout
ruff
boto3
