Example:

cd ~/Billiards-AI && bash scripts/jetson_capture_training_frames.sh --count 300 --stride 20 --prefix set_a_stripes

Then label → move images + matching .txt into images/train & labels/train (and a val split) → jetson_yolo_train.sh.

Docs: docs/ORIN_NANO_TRAIN_AND_TEST.md (“Live table → same training pipeline”) and docs/MODEL_OPTIMIZATION.md. Shipped in commit 49b1080.
